spring:
  application:
    name: asynchronous-request-reply-pattern

# Config usada apenas nos testes da própria lib
async-jobs:
  retention: PT1H
  retry-after-seconds: 5

logging:
  level:
    org.springframework.web: INFO
